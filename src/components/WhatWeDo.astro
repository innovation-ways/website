---
const pillars = [
  {
    title: "Telecom BSS engineering",
    body:
      "Two decades designing and owning mission-critical billing and BSS systems at scale — sole developer of an invoicing system that produces millions of invoices a month, across successive platform upgrades, with zero major production incidents.",
  },
  {
    title: "Applied AI, production-grade",
    body:
      "Bringing the same discipline to AI systems — orchestration, retrieval, evaluation, and observability — and building it in the open. The work, not the pitch, is the proof.",
  },
];
---

<section class="border-t border-line">
  <div class="mx-auto max-w-6xl px-6 py-16 sm:py-20">
    <h2 class="text-2xl font-semibold sm:text-3xl">What Innovation Ways does</h2>
    <div class="mt-10 grid gap-10 sm:grid-cols-2">
      {
        pillars.map((p) => (
          <div class="reveal">
            <h3 class="text-lg font-semibold text-ink">{p.title}</h3>
            <p class="mt-3 text-muted">{p.body}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
